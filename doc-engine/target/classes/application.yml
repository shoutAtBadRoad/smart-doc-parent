server:
  port: 8080

spring:
  application:
    name: smart-doc-engine

# 文件上传配置
servlet:
  multipart:
    max-file-size: 10MB
    max-request-size: 10MB

# 文档生成配置
doc:
  template-path: ./templates
  output-path: ./output
